FROM mcr.microsoft.com/devcontainers/base:noble

# Template note: rendered projects receive this content from Dockerfile.jinja
# (this copy is kept only so the template repository remains usable locally).

# Install uv
COPY --from=ghcr.io/astral-sh/uv:latest /uv /bin/uv
